## 🔧 Install

```bash
# Optional: create virtual environment
python -m venv venv
source venv/bin/activate

uv pip install pytest

cd transforms

uv pip install ".[all]"

cd ../data-process-lib
uv pip install . 
```
