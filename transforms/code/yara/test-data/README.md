# Test data

`input/sample.parquet` and `expected/sample.parquet` are generated by
`scripts/make_test_data.py` from this transform directory. Regenerate them
after changing rules, expected schema, or test payloads:

```bash
python scripts/make_test_data.py
```
