# Ray enablement of python multimodal transforms

To setup test environment and ability to build images,

```shell
make venv
make cp-python
```
To make the image with all transforms and publish it 

```shell
make image publish
```
